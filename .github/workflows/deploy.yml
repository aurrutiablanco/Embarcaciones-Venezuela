name: Deploy to Production VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Execute remote SSH deployment
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /var/www/ev4.antoniomarcoweb.com/public_html

            # 1. Tomar propiedad temporal como superusuario para evitar bloqueos
            sudo chown -R $USER:$USER .
            sudo git config --global --add safe.directory /var/www/ev4.antoniomarcoweb.com/public_html

            # 2. Forzar sincronización exacta con main purgiando binarios antiguos
            git fetch origin main
            git reset --hard origin/main
            git clean -fd
            git lfs pull

            # 3. Restaurar permisos estrictos de seguridad para Apache
            sudo chown -R www-data:www-data .
            sudo chmod -R 775 .

            # 4. Recargar servidor
            sudo systemctl reload apache2
